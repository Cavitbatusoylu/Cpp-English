#include <iostream>
using namespace std;

int main()
{
	// C++ break and continue usage
	//
	// break = exits the loop completely
	// continue = skips the remaining code in the loop for that iteration and continues with the next one

	// BREAK
	/*
	for (int i = 0; i <= 5; i++)
	{
		if (i == 3)
			break;

		cout << i << " ";
	}
	*/

	// CONTINUE
	/*
	for (int i = 0; i <= 5; i++)
	{
		if (i == 3)
			continue;

		cout << i << " ";
	}
	*/

	cout << endl;
}
